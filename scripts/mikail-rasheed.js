const aaloleHngDetails = () => {
  const name = "Rasheed Mikail";
  const id = "HNG-02286";
  const email = "Rasheedabiodun556@yahoo.com";
  const lang = "JavaScript";
  console.log(
    `Hello World, this is ${name} with HNGi7 ID ${id} and email ${email} using ${lang} for stage 2 task.`
  );
  return `Hello World, this is ${name} with HNGi7 ID ${id} and email ${email} using ${lang} for stage 2 task.`;
};

aaloleHngDetails();
