const info = {
    name: "Tefe Mebuiefene",
    id: "HNG-02840",
    email: "tefeglowree@yahoo.com",
    language: "JavaScript",
};

function output() {
    const message = `Hello World, this is ${info.name} with HNGi7 ID ${info.id} and email ${info.email} using ${info.language} for stage 2 task.`;
    console.log(message);
}
output();
