//Declearation of variable
var fullName = `Francis Obadiah`, ID = `HNG-00072`, language = `javascript`, email = `francisobadiahajigana@gmail.com`;

//Output Definition
const outPut =`Hello World, this is ${fullName} with HNGi7 ID ${ID} and email ${email} using ${language} for stage 2 task.`;

//defining the function to
function outputScript() {
    //logging output
    console.log(outPut);
}

//function call
outputScript();
