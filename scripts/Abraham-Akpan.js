// storing variables in an object
const intern = {
  firstName: 'Abraham',
  lastName: 'Akpan',
  hngId: 'HNG-01927',
  language: 'JavaScript',
  email: 'akpjunio4real@gmail.com'
}

// output show message
const showMessage = `Hello World, this is ${intern.lastName} ${intern.firstName} with HNGi7 ID ${intern.hngId} and email ${intern.email} using ${intern.language} for stage 2 task.`;

console.log(showMessage);                                                                                 
